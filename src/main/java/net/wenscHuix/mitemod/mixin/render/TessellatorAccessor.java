package net.wenscHuix.mitemod.mixin.render;

/**
 * Minimal stub to satisfy classpath references created by dumped classes in libs/DumpClasses.jar.
 * The real project originally provided this via a mixin; we only need the type to exist for compilation.
 */
public interface TessellatorAccessor {
    // Intentionally empty: this is a stub to allow compilation. If concrete methods are required
    // by other code at compile time, we'll add them here.
}